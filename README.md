# Getting Started

If you want to use this as a Chrome unpacked extension:

1. Inside Chrome, open chrome://extensions/ .
2. Enable the `Developer mode` toggle .
3. Press `Load unpacked` and select the project directory from your local file system .
